# Toonflow 生产镜像构建上下文忽略规则
# 构建上下文为 monorepo 根目录；后端镜像只需 backend/ 与 deploy/，其余全部排除以减小上下文体积。

**/.git
**/.gitignore
**/node_modules
**/dist
frontend
docs
*.log
*.md
.env
deploy/.env
deploy/backup

# 运行时数据（不应进入镜像）
backend/data/db2.sqlite
backend/data/oss
backend/data/assets
backend/data/skills
backend/data/version.txt
# 注意：backend/data/serve 不再排除——SKIP_BUILD 模式下其中放预构建的 app.js bundle（纯 JS 架构无关），
#       需随构建上下文进入镜像；非 SKIP_BUILD 模式由 esbuild 在镜像内生成，无需外部提供。
